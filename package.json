{
  "name": "password-hash",
  "version": "1.2.0",
  "description": "CLI to generate an Argon2id hash from a password.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "argon2": "^0.43.0"
  },
  "devDependencies": {
    "semantic-release": "^24.0.0",
    "@semantic-release/changelog": "^6.0.0",
    "@semantic-release/git": "^10.0.0"
  }
}
